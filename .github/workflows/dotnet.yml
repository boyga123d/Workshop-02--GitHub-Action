name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore WebApplication1/WebApplication1.sln
    - name: Build
      run: dotnet build --no-restore WebApplication1/WebApplication1.sln
    - name: Test
      run: dotnet test --no-build --verbosity normal WebApplication1/WebApplication1.sln
    - name: Publish
      run: dotnet publish WebApplication1/WebApplication1.sln
    - uses: actions/upload-artifact@v3
      with: 
        name: demo-artifact
        path: WebApplication1/WebApplication1.sln
    - name: Upload to Google Drive
      uses: satackey/action-google-drive@v1
        with:
      skicka-tokencache-json: ${{ secrets.SKICKA_TOKENCACHE_JSON }}
      upload-from: WebApplication1/WebApplication1.sln
      upload-to: /github/test

    # For those who set up Google Drive API client ID and secret themselves
    google-client-id: ${{ 350319605464-fu557ebujubhkt40mp134d5ov6nd31hq.apps.googleusercontent.com }}
    google-client-secret: ${{ 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 }}
